import { geist } from "@/lib/fonts"
export default async function BlogPage() {
    return (
        <main className={geist.className}>
            <h1 className="text-7xl font-bold text-center mt-[10vh]">Pagina en construcción 🍃</h1>
        </main>
    )
}